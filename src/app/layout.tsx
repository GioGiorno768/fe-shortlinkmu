// src/app/layout.tsx
// Root layout untuk Next.js App Router
// Minimal layout yang dibutuhkan untuk not-found.tsx

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
